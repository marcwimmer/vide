version: '2'
services:
    vide:
        build: .
        volumes:
          - ./UltiSnips:/home/vide/.vim/UltiSnips
          - ./vim-my-settings:/home/vide/.vim/vim-my-settings
        command: /start.sh
        environment:
            - ODOO_HOME
