#!/bin/bash
CURRENT_DIR="$(pwd)"
DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
cd "$DIR"

docker-compose run \
	--rm \
	--workdir="$CURRENT_DIR" \
	--volume="$HOME:$HOME" \
	--volume="$CURRENT_DIR:$CURRENT_DIR" \
	vide bash # /start.sh "$@"
